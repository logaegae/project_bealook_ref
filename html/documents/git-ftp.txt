git config git-ftp.url "223.26.138.5"
git config git-ftp.user "logaegae"
git config git-ftp.password "123qweasd"

# Upload all files
git ftp init

# Or if the files are already there
git ftp catchup

# Work and deploy
echo "new content" >> index.txt
git commit index.txt -m "Add new content"

# 단축키
terminal : cmd+shift+T
git command : cmd+shift+H

git ftp push
